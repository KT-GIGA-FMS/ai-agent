# =============================================================================
# 차량 예약 AI 에이전트 환경변수 설정
# =============================================================================
# 이 파일을 .env.local로 복사한 후 실제 값으로 설정하세요:
# cp .env.example .env.local

# =============================================================================
# Azure OpenAI 설정 (필수)
# =============================================================================
# Azure OpenAI 리소스에서 가져온 설정값들
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=your_deployment_name
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# =============================================================================
# 데이터베이스 설정 (필수)
# =============================================================================
# 로컬 개발용 PostgreSQL
DATABASE_URL=postgresql://reservation_user:reservation_password@localhost:5432/reservation_db

# =============================================================================
# Redis 설정 (필수)
# =============================================================================
# 로컬 개발용 Redis
REDIS_URL=redis://localhost:6379/0

# =============================================================================
# 애플리케이션 설정 (선택사항)
# =============================================================================
# 로그 레벨: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 환경: development, staging, production
ENVIRONMENT=development

# =============================================================================
# Azure 배포용 설정 (Azure 배포 시 사용)
# =============================================================================
# Azure Database for PostgreSQL
# DATABASE_URL=postgresql://username:password@your-server.postgres.database.azure.com:5432/reservation_db?sslmode=require

# Azure Cache for Redis
# REDIS_URL=rediss://username:password@your-server.redis.cache.windows.net:6380?ssl=True

# =============================================================================
# 설정 가이드
# =============================================================================
# 1. Azure OpenAI 설정:
#    - Azure Portal에서 OpenAI 리소스 생성
#    - 모델 배포 후 API 키와 엔드포인트 확인
#    - 배포 이름 설정 (예: gpt-4, gpt-35-turbo)
#
# 2. 로컬 개발:
#    - docker-compose up으로 PostgreSQL/Redis 실행
#    - DATABASE_URL과 REDIS_URL은 기본값 사용
#
# 3. Azure 배포:
#    - Azure Database for PostgreSQL 생성
#    - Azure Cache for Redis 생성
#    - 해당 서비스의 연결 문자열로 변경
#
# 4. 보안 주의사항:
#    - .env.local 파일은 절대 Git에 커밋하지 마세요
#    - 프로덕션에서는 Azure Key Vault 사용 권장
